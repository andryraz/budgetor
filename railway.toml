[build]
builder = "nixpacks"

[deploy]
startCommand = "npm install && npm run build && npx prisma migrate deploy && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environment]
NODE_VERSION = "20"